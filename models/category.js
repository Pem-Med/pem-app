class Category{
    constructor(id, title, icon, tileColor){
        this.id = id;
        this.title = title;
        this.icon = icon;
        this.tileColor = tileColor;
    }
}

export default Category;